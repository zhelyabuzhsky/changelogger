build:
  docker:
    web: Dockerfile
run:
  web: - gunicorn
         - --bind=0.0.0.0:8000
         - --certfile=certs/development/server.crt
         - --keyfile=certs/development/server.key
         - --access-logfile=-
         - --error-logfile=-
         - --log-level=info
         - --reload
         - changelogger.wsgi
